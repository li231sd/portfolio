root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["web", "static", "assets", "tmp"]

[log]
  time = false

[misc]
  clean_on_exit = true
  